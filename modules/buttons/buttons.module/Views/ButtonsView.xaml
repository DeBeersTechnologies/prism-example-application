<UserControl x:Class="buttons.Views.ButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" 
             HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 10">
    <StackPanel Orientation="Vertical">
        <Button Margin="5 5" Width="60" Command="{Binding ExitApplicationCommand}">Exit</Button>
        <Button Margin="5 5" Width="60" Command="{Binding ReloadApplicationCommand}">Update</Button>
        <Button Margin="5 5" Width="60" Command="{Binding RollbackApplicationCommand}">Roll back</Button>
    </StackPanel>
</UserControl>
